#pragma once

// A private implementation header, in the `bits/` directory every C library
// keeps its arch/detail split in. No consumer may include this directly; the
// public header that owns the type includes it.

typedef long __probe_off;
